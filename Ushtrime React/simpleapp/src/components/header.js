function header(props) {
    return (
        <>
            <header>
                <h1>{props.title}</h1>
                <p>{props.desc}</p>
            </header>


        </>
    )
}

export default header